{
  "name": "harbour-contracts",
  "version": "1.0.0",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "dependencies": {
    "@safe-global/safe-contracts": "^1.4.1-2",
    "@safe-global/safe-singleton-factory": "^1.0.43"
  },
  "devDependencies": {
		"@nomicfoundation/hardhat-toolbox": "^5.0.0",
		"@nomicfoundation/hardhat-verify": "^2.0.13",
		"dotenv": "^16.5.0",
		"hardhat": "^2.23.0",
		"hardhat-deploy": "^1.0.2",
		"hardhat-gas-reporter": "^1.0.8"
  },
  "scripts": {
    "build": "hardhat compile",
    "deploy": "hardhat deploy-and-verify --network",
    "lint": "echo 'No linting rules defined'",
    "format": "echo 'No formatting rules defined'",
    "test": "hardhat test",
    "test:bench": "hardhat test --grep '@bench'",
    "hardhat:clean": "hardhat clean"
  },
  "keywords": [
    "Safe",
    "Harbour"
  ],
  "author": "",
  "license": "LGPL-3.0-only",
  "description": "",
  "overrides": {
    "@safe-global/safe-contracts": {
      "ethers": "^6.13.5"
    }
  }
}
